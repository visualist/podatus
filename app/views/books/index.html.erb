<div class="books-page">
  <% @books.each do |book| %>
    <div class="book-item">
      <div class="book-title"><%= book.has_data? ? link_to(book.title, book_path(book)) : book.title %></div>

      <div class="row">
        <div class="chapter"><p>2</p>
        </div>
        <div class="graph">
          <!-- horizontal stream graph goes here -->
        </div>
        <div class="clearfix"></div>
      </div>

    </div>
  <% end %>
</div>
